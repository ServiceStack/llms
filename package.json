{
    "scripts": {
        "test": "python -m unittest discover -s tests -p 'test_*.py'",
        "ui:dev": "tailwindcss -i ./llms/ui/tailwind.input.css -o ./llms/ui/app.css --watch",
        "ui:build": "tailwindcss -i ./llms/ui/tailwind.input.css -o ./llms/ui/app.css --minify",
        "build": "python publish.py --build --prod",
        "version": "python -c \"from publish import get_current_version; print(get_current_version())\"",
        "bump": "python publish.py --bump",
        "release": "python publish.py --release",
        "lint": "ruff check .",
        "lint:fix": "ruff check --fix .",
        "lint:fix:unsafe": "ruff check --fix --unsafe-fixes .",
        "format": "ruff format .",
        "format:check": "ruff format --check ."
    }
}